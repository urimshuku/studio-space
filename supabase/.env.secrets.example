# Copy to `.env.secrets` and fill in (that file is gitignored).
# Push to Supabase:
#   npx supabase secrets set --env-file supabase/.env.secrets
#
RESEND_API_KEY=re_your_key_from_resend
BOOKING_ADMIN_EMAIL=you@yourdomain.com
# Optional — override default "From" in book-venue/join-activity email.tsx
# BOOKING_FROM_EMAIL=Studio Space <bookings@studiospace.community>

# Public URL of the deployed SPA (no trailing slash). Used in email footers for
# unsubscribe + email-preferences links. Not a VITE_* variable — Edge only.
# If emails lack footer links, verify this secret first.
PUBLIC_APP_URL=https://your-public-site.example

# PayPal Checkout (PayPal / card on the donation form)
# PAYPAL_CLIENT_ID=
# PAYPAL_CLIENT_SECRET=
# PAYPAL_MODE=live
